spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mollusc
    username: postgres
    password: db-wrz2z
    continue-on-error: true
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect